if (<%= @owned_stock.quantity %> > 0){
	$('.stock-row#<%= @owned_stock.id %>').html('<%= j(render partial: "portfolios/owned_stock", object: @owned_stock) %>');
} else {
	$('.stock-row#<%= @owned_stock.id %>').remove();
}
$('#portfolio-cash').html('<%= number_to_currency(@portfolio.cash) %>')
